import React, { Component } from "react";
import { connect } from "react-redux";

class Support extends Component {
  render() {
    return <div>I AM SUPPORT PAGE</div>;
  }
}

export default connect()(Support);
